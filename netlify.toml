[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# Single-page app: every route falls back to index.html so deep links work.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
